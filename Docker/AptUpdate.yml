---
- name: Update Packages
  hosts: all
  become: true

  tasks:
    - name: Update
      when: ansible_pkg_mgr == 'apt'
      ansible.builtin.apt:
        update_cache: true

    - name: Upgrade
      when: ansible_pkg_mgr == 'apt'
      ansible.builtin.apt:
        upgrade: dist
